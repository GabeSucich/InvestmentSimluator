const Utils = {

    // This function returns an array of dates in the stockData that are within SPAN days of the currentDate
    getPreviousDates(currentDate, stockData, span) {
        const recentDates = []
        const dateArr = Object.keys(stockData)
        var dateIndex;
        for (const date of dateArr) {
            if (date === currentDate) {
                
            }
        }
    }

}

export default Utils